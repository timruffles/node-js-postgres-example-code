{
  "name": "code",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": ">= 6.0.0"
  },
  "scripts": {
    "db:create": "createuser rdbs-node && createdb rdbs-node -O rdbs-node",
    "test": "mocha tests",
    "test:ci": "npm run -s db:create && mocha tests",
    "seed-one": "node .util/run-seed 01_initial"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "knex": "^0.7.3",
    "mocha": "^2.1.0",
    "pg": "^4.2.0"
  },
  "devDepedencies": {
    "lodash": "*"
  }
}
